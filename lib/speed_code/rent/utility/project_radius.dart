import 'package:flutter/material.dart';

mixin ProjectRadius {
  static BorderRadius borderRadiusCircularNormal = BorderRadius.circular(20);
  static BorderRadius borderRadiusCircularAllNormal =
      const BorderRadius.all(Radius.circular(8));
}
